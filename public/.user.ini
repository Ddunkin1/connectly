; Increase limits for video uploads (up to 50MB)
; If video uploads still fail: Laravel Herd users - open Herd app, go to PHP tab, 
; click "Open php.ini" and set upload_max_filesize=50M, post_max_size=52M
upload_max_filesize = 50M
post_max_size = 52M
memory_limit = 256M
max_execution_time = 120
